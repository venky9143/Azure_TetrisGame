apiVersion: apps/v1
kind: Deployment
metadata:
  name: tetris-deployment
  labels:
    app: tetris
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tetris
  template:
    metadata:
      labels:
        app: tetris
    spec:
      containers:
      - name: tetris
        image: IMAGE_PLACEHOLDER   # pipeline replaces this dynamically
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
